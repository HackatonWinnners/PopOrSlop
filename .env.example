# Postgres (docker compose defaults)
DATABASE_URL=postgres://poporslop:poporslop@localhost:5433/poporslop
TEST_DATABASE_URL=postgres://poporslop:poporslop@localhost:5433/poporslop_test

# Session cookie signing secret (generate: openssl rand -hex 32)
SESSION_SECRET=change-me

# W1+: magic-link email delivery
RESEND_API_KEY=

# Workers (W1+)
SEC_USER_AGENT="PopOrSlop admin@example.com"
CH_API_KEY=
